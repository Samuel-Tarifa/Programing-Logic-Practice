/* Dada una cadena S, escribe una función que encuentre la subcadena (substring) más larga que aparezca al menos dos veces en S. La
subcadena debe ser continua y no debe solaparse. La función debe devolver la subcadena más larga. Si hay múltiples subcadenas con la misma
longitud máxima, devuelve cualquiera de ellas. Si no hay ninguna subcadena que se repita, devuelve una cadena vacía.
*/
function longestSubstring(s){
  let result=''
  for (let i=0;i<s.length;i++){
    
  }
  return result
}

console.log(longestSubstring(`abcab`)); // ab